name: Deploy

# Manual deployment only — SaaS is deployed via SCP to the server.
# This workflow is kept as a placeholder for future CI/CD automation.
on:
  workflow_dispatch:

jobs:
  placeholder:
    name: Manual deploy (not automated yet)
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy is done manually via SCP. See MEMORY.md for instructions."
